---
- name: 40-verify - Apply pending containerd restart
  ansible.builtin.meta: flush_handlers

- name: 40-verify - Verify NVIDIA Container Toolkit
  ansible.builtin.command: nvidia-container-cli info
  register: nvidia_container_cli_info
  changed_when: false
