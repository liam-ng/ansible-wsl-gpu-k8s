---
- name: 20-install - Install containerd
  ansible.builtin.apt:
    name: "{{ containerd_package_name }}={{ containerd_version }}"
    state: present
    update_cache: true

- name: 20-install - Hold containerd package version
  ansible.builtin.dpkg_selections:
    name: "{{ containerd_package_name }}"
    selection: hold
